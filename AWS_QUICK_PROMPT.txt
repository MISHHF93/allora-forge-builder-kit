╔════════════════════════════════════════════════════════════════════════════╗
║                    ALLORA FORGE - AWS DEPLOYMENT                           ║
║                  Topic 67: 7-Day BTC/USD Log-Return                        ║
║                  Competition: Sep 16 - Dec 15, 2025                        ║
╚════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════
📋 COPY THIS ENTIRE BLOCK TO YOUR AWS INSTANCE
═══════════════════════════════════════════════════════════════════════════

# 1. CLONE AND SETUP
cd ~
git clone https://github.com/MISHHF93/allora-forge-builder-kit.git
cd allora-forge-builder-kit

# 2. INSTALL DEPENDENCIES
pip install -r requirements.txt

# 3. CONFIGURE WALLET (REPLACE WITH YOUR MNEMONIC)
echo "word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12 word13 word14 word15 word16 word17 word18 word19 word20 word21 word22 word23 word24" > .allora_key
chmod 600 .allora_key

# 4. SET ENVIRONMENT VARIABLES (REPLACE WITH YOUR VALUES)
export ALLORA_WALLET_ADDR="allo1xxxxxxxxxxxxxxxxxxxxxxxxxx6vma"
export ALLORA_API_KEY="your_api_key_here"
echo 'export ALLORA_WALLET_ADDR="allo1xxxxxxxxxxxxxxxxxxxxxxxxxx6vma"' >> ~/.bashrc
echo 'export ALLORA_API_KEY="your_api_key_here"' >> ~/.bashrc
source ~/.bashrc

# 5. VERIFY CONFIGURATION
python3 tools/print_wallet_address.py
python3 -c "import train; print('✅ Ready to deploy')"

# 6. START WORKER
./start_worker.sh

# 7. MONITOR
./monitor.sh          # Status snapshot
./watch_live.sh       # Live dashboard
tail -f pipeline_run.log   # Follow logs

═══════════════════════════════════════════════════════════════════════════
✅ WORKER NOW RUNNING
═══════════════════════════════════════════════════════════════════════════

The pipeline will:
  ✅ Train XGBoost models every hour (28-day BTC/USD data)
  ✅ Submit predictions at HH:00:00 UTC when window opens
  ✅ Handle REST API 501 errors gracefully with fallbacks
  ✅ Run autonomously through December 15, 2025

Key Features:
  ✅ Window detection fixed (600 blocks = ~50 minutes)
  ✅ Graceful 501 handling (reputers_count=1, stake=0.0)
  ✅ Hourly cadence alignment (no drift)
  ✅ Lifecycle tracking with JSON artifacts
  ✅ Automatic retry on transient failures

Files:
  📁 pipeline.pid           → Worker process ID
  📝 pipeline_run.log       → Execution logs
  📊 submission_log.csv     → Submission history
  🗂️  data/artifacts/logs/   → Lifecycle JSON files

Commands:
  pkill -f "python.*train"  → Stop worker
  ./start_worker.sh         → Start worker
  cat pipeline.pid          → Show worker PID
  ps -p $(cat pipeline.pid) → Check if running

═══════════════════════════════════════════════════════════════════════════
📚 FULL DOCUMENTATION
═══════════════════════════════════════════════════════════════════════════

See AWS_DEPLOYMENT_GUIDE.md for:
  • Detailed setup instructions
  • Systemd service configuration
  • Troubleshooting guide
  • Monitoring best practices
  • Maintenance procedures

Run: cat AWS_DEPLOYMENT_GUIDE.md

═══════════════════════════════════════════════════════════════════════════
🔒 SECURITY REMINDERS
═══════════════════════════════════════════════════════════════════════════

  ⚠️  Keep .allora_key secure (contains mnemonic)
  ⚠️  Use chmod 600 .allora_key
  ⚠️  Never commit .allora_key to git
  ⚠️  Backup wallet to secure location
  ⚠️  Restrict SSH access to your IP only

═══════════════════════════════════════════════════════════════════════════

Questions? Check logs first:
  tail -50 pipeline_run.log
  
Common statuses in submission_log.csv:
  ✅ success                    → Submission accepted
  ⏸️  skipped_window            → Window closed (wait for next hour)
  ⏸️  skipped_topic_not_ready   → Topic not active yet
  ⏸️  filtered_high_loss        → Model loss too high (auto-filtered)
  ⏸️  cooldown_600s             → Recent submission within 10 min

All "skipped" statuses are NORMAL and handled gracefully.
Worker will retry automatically in next iteration.

═══════════════════════════════════════════════════════════════════════════
